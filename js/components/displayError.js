function displayError(message = "Unknown error") {
    return `<div class="results errorMes">
                <div>
                    <h2>Sorry...</h2>
                    <p>${message}</p>
                </div>
            </div>`;
}
